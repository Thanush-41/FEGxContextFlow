The deployed `index.ts` is generated by `scripts/build-edge.mjs` from `server/edge/handler.ts` and the shared action engine. Edit those sources, then rebuild. Azure and Supabase privileged credentials are Edge Function secrets, never bundled constants.
